str1 = "Hello"
str2 = "World"
result = str1 + " " + str2        #Concate the strings
print(result)
print(result.upper())             # Changing case from lower to upper
length = len(result)            #defining length variable.
print("length of string:", length) # capture and display length of the string